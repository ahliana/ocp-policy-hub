<!-- proofmark-ship head=501e007385547bc5de6da22f809fcd90ddee3101 -->
# Report - OCP-CE-HR-Policy-Searcher, main..HEAD

## What shipped
- 501e007 ci: install with constraints.txt so CI lints under the verified ruff
- 9e51c2f docs(report): ship report for the CI workflow
- a6ce105 ci: test workflow for PRs + contributor testing docs

5 files changed, 139 insertions(+), 5 deletions(-)

## Test floor
1869 -> 1869  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 8848 1235 86%  measured 2026-08-26T05:46:23+00:00

## What the gates did
passes 18, bound 4, blocks 3, overrides 0, exceptions 0
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm
    block     ship-report: no report for 2 commit(s)

## Rollback
    git revert --no-edit 6f79bcc68865..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: the ruff lint contract now pins 0.15.20 via constraints; upgrading the linter (and fixing the ~dozen findings 0.16's defaults add) is a deliberate future task, not this push. The autocrlf/ring-stale trap in the other six ring repos and the installer remains a canonical-side question. The superseded report a6ce105a2624 stays in gates/reports/ as history.
- The diagnosis you got wrong first, and what corrected it: assumed CI's backend failure would be the Python 3.12 suite (the risk named in the previous report); the log showed pytest never ran - lint failed first, because unconstrained pip resolved ruff 0.16.4 against the local 0.15.20. Reading which STEP failed corrected it.
- Numbered open questions: 1. When to upgrade the pinned ruff and adopt 0.16's expanded defaults? 2. Should the Dockerfile's and CI's shared constraints contract also cover the weekly/monthly scheduled workflows (they currently install unconstrained)?
- Verified live by fetching real content (not a status code)? what, and what did it say: PR #30's checks are the live surface: frontend job GREEN on the first run (35 suites on Node 22 from a cold clone); backend re-run with constraints pending at report time - landing on main waits for both jobs green by name, which supersedes this question's usual deploy-grep.
