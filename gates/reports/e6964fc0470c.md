<!-- proofmark-ship head=e6964fc0470cdc3fcabbab02645a76f7d12129ae -->
# Report - OCP-CE-HR-Policy-Searcher, main..HEAD

## What shipped
- e6964fc fix: hoist the size-plugin's stdlib imports to the top of conftest
- 0f704c2 docs(report): ship report for the constraints fix
- 501e007 ci: install with constraints.txt so CI lints under the verified ruff
- 9e51c2f docs(report): ship report for the CI workflow
- a6ce105 ci: test workflow for PRs + contributor testing docs

7 files changed, 177 insertions(+), 11 deletions(-)

## Test floor
1869 -> 1869  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 8848 1235 86%  measured 2026-08-26T05:53:51+00:00

## What the gates did
passes 23, bound 6, blocks 3, overrides 0, exceptions 0
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm
    block     ship-report: no report for 2 commit(s)

## Rollback
    git revert --no-edit 6f79bcc68865..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: the local process gap that let E402 through - the commit gate's ruff deliberately runs only the swallow rules, so the repo's own full lint has no local enforcement point; CI is now that point for PRs, but a local commit straight to main would still miss it. Left as-is: adding full ruff to pre-commit is a canonical/ring decision, not a one-repo patch. The installer also writes the mid-file imports that trip E402 - canonical-side fix noted for repo 9.
- The diagnosis you got wrong first, and what corrected it: expected the second backend failure to be the 3.12 suite; it was still Lint - two E402s in the installer-wired conftest block, a real finding this time (same pinned ruff as local), corrected by reading the two error lines rather than the step name.
- Numbered open questions: 1. Should canonical's installer emit the conftest wiring with hoisted stdlib imports (this fix, upstreamed)? 2. Should the ring's pre-commit run the repo's own ruff config where one exists, so full-lint has a local enforcement point too?
- Verified live by fetching real content (not a status code)? what, and what did it say: local ruff at the pinned 0.15.20 says "All checks passed!" and the canary/docs-gating tests still pass (4 passed, 1 xfailed); the authoritative live check is PR #30's backend job on this push - landing waits for it green by name.

## Escapes to analyse - to be written by a person

One block per escape. A run of fix commits on one day for one thing is
one escape, not several. `none` is a real answer to the first question -
some defects no mechanical gate could have caught, and saying so is the
analysis. The second is yes or no.

### escape 2026-08-26 unclassified (1 fix commit)
- Which gate should have caught it (name one, or `none`): none
- Did that gate already exist and pass anyway (yes/no): no

Analysis: no local gate covers E402 by design (ruff-swallow runs only the swallow rules). The gate that should catch this class is CI's Lint step - introduced by this very branch, and it did catch it on its first constrained run.
