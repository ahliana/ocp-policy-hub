<!-- proofmark-ship head=7278c3a0c6c940b215592bd0919065778fd133ab -->
# Report - OCP-CE-HR-Policy-Searcher, main..HEAD

## What shipped
- 7278c3a chore(gates): adopt the untested-module gate
- 00f231e chore(gates): install the Proofmark commit ring
- 61eb12b chore(tests): size-mark slow suites large; drop dead CLI stub
- 5b7db60 test: real assertions for assert-free tests; hermetic docs-gating reloads

30 files changed, 3902 insertions(+), 94 deletions(-)

## Test floor
n/a -> 1869  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 8848 1235 86%  measured 2026-08-26T04:15:42+00:00

## What the gates did
passes 2, bound 2, blocks 0, overrides 0, exceptions 0

## Rollback
    git revert --no-edit 5a7ad2f6a580..HEAD
    # then restore gates/min_test_count.txt to n/a; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: src/mcp/server.py executes zero statements under any test - grandfathered as the entire untested-module baseline rather than tested today. src/api/app.py's import-time load_dotenv(override=True) makes any module-reloading test env-sensitive; fixed in the one test file it bit (test_docs_gating), the import-time load itself left alone. Four tests kept semantically-redundant literal asserts beside their mock assert_* calls because the AST gate cannot see mock methods.
- The diagnosis you got wrong first, and what corrected it: four unit failures after the branch landing looked like the new test edits; they were a stale gitignored frontend/build/ from 17 July (the app auto-serves it, flipping root and docs-gating routes) plus the developer .env re-injected by reload. Deleting the stale build and reading app.py line 29 corrected both.
- Numbered open questions: 1. Should the app serve frontend/build only behind an explicit env flag so the suite stops depending on checkout hygiene? 2. Does OCP want the contributors' Running-the-tests paragraph in CONTRIBUTING.md plus a ci.yml on PRs (the community enforcement half), and does anyone there want gates/wire.py documented beyond CLAUDE.md? 3. Should src/mcp/server.py get a smoke test so the untested baseline reaches zero?
- Verified live by fetching real content (not a status code)? what, and what did it say: not applicable - nothing here deploys; the deployed site is unchanged. The gates were verified by firing: the install commit and adoption commit each ran the full pre-commit (PROOFMARK PASS 91.4s / 85.8s), canonical's installer self-proof passed 34/34, and doctor proves both venvs' pinned toolchains.
