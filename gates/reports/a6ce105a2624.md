<!-- proofmark-ship head=a6ce105a2624e3b9cd3631c9ee51eed3b7b8cad7 -->
# Report - OCP-CE-HR-Policy-Searcher, main..HEAD

## What shipped
- a6ce105 ci: test workflow for PRs + contributor testing docs

3 files changed, 106 insertions(+), 4 deletions(-)

## Test floor
1869 -> 1869  (unchanged)

## Coverage (reported fact, never a gate)
TOTAL 8848 1235 86%  measured 2026-08-26T04:40:23+00:00

## What the gates did
passes 11, bound 2, blocks 2, overrides 0, exceptions 0
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm
    block     ring-stale: this repo is running a gate the ring has moved past: ['check_test_asserts.py', 'doctor.py', 'gate.py', 'proofm

## Rollback
    git revert --no-edit 6f79bcc68865..HEAD

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: check_ring_current hashes distributed files byte-for-byte, so ANY ring repo with autocrlf that re-checks-out gates/** self-blocks as ring-stale. Fixed here with .gitattributes; the other six repos carry the same latent trap and the installer does not write the attributes file. Left for a canonical-side fix (installer writes .gitattributes, or ring hashing normalizes newlines).
- The diagnosis you got wrong first, and what corrected it: read the block as real drift ("the ring has moved past") before checking bytes - od showed the only difference was CRLF from the checkout smudge; canonical had not changed any distributed file since install. ring.py sync restored LF bytes and the six healthy repos reported already-current, confirming no real drift existed.
- Numbered open questions: 1. Canonical: should install.py write the gates/** eol=lf attribute so repo 9 is not born with this trap? 2. Should ring hashing normalize line endings instead? (Attributes chosen here because bytes-on-disk staying canonical is the stronger property.) 3. CI runs the suite on Python 3.12 (production's version) while local dev runs 3.13 - if the PR's CI run goes green, 3.12 compatibility is proven for the first time; if not, that is the finding.
- Verified live by fetching real content (not a status code)? what, and what did it say: the CI workflow's own first run on PR #30 is the live verification and is pending at report time - the PR will show backend and frontend jobs; landing waits for them to be green, which is the content check (job names + conclusions, not a status code).
