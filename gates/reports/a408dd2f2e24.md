<!-- proofmark-ship head=a408dd2f2e24ad14853fe857a05473cd3dd9ac1b -->
# Report - OCP-CE-HR-Policy-Searcher, ocp/main..HEAD

## What shipped
- a408dd2 feat(aispend): record what PolicyPulse spends on the Anthropic API

9 files changed, 571 insertions(+), 8 deletions(-)

## Test floor
2274 -> 2296  (ratcheted)

## Coverage (reported fact, never a gate)
TOTAL 9932 1243 87%  measured 2026-08-28T05:25:24+00:00

## What the gates did
passes 11, bound 2, blocks 2, overrides 0, exceptions 1
    block     push-toctou: pushing 593b0ee3a327 but the worktree is at 70cdd333f066 - the suite about to run proves nothing about the pus
    block     push-toctou: pushing 593b0ee3a327 but the worktree is at 70cdd333f066 - the suite about to run proves nothing about the pus
    exception push-toctou: pushing 593b0ee3a327 but the worktree is at 70cdd333f066 - the suite about to run proves nothing about the pus

## Rollback
    git revert --no-edit 70cdd333f066..HEAD
    # then restore gates/min_test_count.txt to 2274; the floor does not fall on its own

## To be written by a person - the tool cannot know these
- What you found and did NOT fix: The four ledgers are four COPIES of the same module, and their PRICES tables will drift the first time Anthropic changes a rate - each repo pins its own values in a test, so drift becomes invisible rather than loud. There is no shared package across these repos and building one was out of scope. Also not done: nothing yet reads all five ledgers together, so answering 'which app' still means running five commands. And no cap - deliberately, since a cap that silently refuses work is a behaviour change she did not ask for. src/signals/news.py::BLE001::_collect is now a stale swallow-baseline entry - my edit made the finding disappear. Regenerating the baseline is a separate chore.
- The diagnosis you got wrong first, and what corrected it: I assumed this app had no usage tracking at all. Listing data/ showed ask_usage.json and legiscan_usage.json already there. Reading ask_usage.json corrected me twice over: it is a per-day request COUNT used as a rate limit, carrying no tokens and no cost - so it neither answered the question nor should be extended to. It answers 'how often' and never 'how much'. A test now pins that distinction so the next person makes neither mistake.
- Numbered open questions: (1) Should a cap follow the tracking, and at what number? (2) Should something read all five ledgers at once, or is per-app enough now that the console's per-key breakdown exists? (3) The four PRICES copies: accept the drift, or find a way to share? (4) Do the estimates match the real invoice once a month of data exists?
- Verified live by fetching real content (not a status code)? what, and what did it say: Not on the server yet - this is the commit, deploy follows. What WAS verified against reality before any code was written: every model id came from grepping each app's real source rather than from assumption. That is what put Opus in the price table at all, since Tad and Sightglass run on it and pricing Opus like Sonnet would understate them five-fold. The seven call sites here were found by reading the source, and the guard test reads it again on every run rather than trusting my list.
